---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - skills/seo/hooks/validate-yaml.py
  - skills/seo/requirements.txt
  - scripts/verify-mcp-scope.sh
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04

must_haves:
  truths:
    - "Running install.sh copies all skill directories to ~/.claude/skills/ and agent files to ~/.claude/agents/ without errors"
    - "Running install.sh creates a Python venv at ~/.claude/skills/seo/.venv/ with all dependencies from requirements.txt"
    - "The YAML validation script exits non-zero when given a SKILL.md with malformed frontmatter"
    - "The YAML validation script exits zero when given a SKILL.md with valid frontmatter"
    - "Python helper scripts (fetch_page, parse_html, capture_screenshot, analyze_visual) execute without import errors inside the venv"
  artifacts:
    - path: "install.sh"
      provides: "Install script for skill system deployment"
      contains: "cp.*skills"
    - path: "skills/seo/hooks/validate-yaml.py"
      provides: "YAML frontmatter validation for all SKILL.md files"
      contains: "yaml.safe_load"
    - path: "scripts/verify-mcp-scope.sh"
      provides: "Verification that MCPs are registered at user scope"
      contains: "mcp.json"
  key_links:
    - from: "install.sh"
      to: "~/.claude/skills/"
      via: "file copy operations"
      pattern: "cp.*skills.*\\.claude/skills"
    - from: "install.sh"
      to: "skills/seo/requirements.txt"
      via: "pip install in venv"
      pattern: "pip install.*requirements"
    - from: "skills/seo/hooks/validate-yaml.py"
      to: "skills/*/SKILL.md"
      via: "glob and yaml.safe_load"
      pattern: "glob.*SKILL\\.md"
---

<objective>
Create the install script, source repository directory layout, YAML validation, Python venv setup, and MCP scope verification for the expanded 27-command SEO skill system.

Purpose: Establish the deployment mechanism so that all skill files can be installed to ~/.claude/ with a single command, Python dependencies are managed via venv, and YAML frontmatter errors are caught before deployment.
Output: install.sh, validate-yaml.py, verify-mcp-scope.sh, updated requirements.txt
</objective>

<execution_context>
@/Users/aash-zsbch1500/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aash-zsbch1500/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- Existing directory structure at ~/.claude/skills/ -->
Existing skill directories (flat pattern):
  seo/          (main orchestrator with SKILL.md, references/, scripts/, hooks/, schema/, pdf/)
  seo-audit/    (SKILL.md)
  seo-page/     (SKILL.md)
  seo-technical/ (SKILL.md)
  seo-content/  (SKILL.md)
  seo-schema/   (SKILL.md)
  seo-images/   (SKILL.md)
  seo-sitemap/  (SKILL.md)
  seo-geo/      (SKILL.md)
  seo-plan/     (SKILL.md)
  seo-programmatic/ (SKILL.md)
  seo-competitor-pages/ (SKILL.md)
  seo-hreflang/ (SKILL.md)

Existing agent files at ~/.claude/agents/:
  seo-content.md, seo-performance.md, seo-schema.md,
  seo-sitemap.md, seo-technical.md, seo-visual.md

Existing Python dependencies (skills/seo/requirements.txt):
  (to be read by executor — add any missing deps for helpers)

SKILL.md frontmatter pattern:
  ---
  name: seo-audit
  description: >
    Multi-line description of the skill...
  allowed-tools:    (optional, not all sub-skills have this)
    - Read
    - Bash
  ---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source repo layout and install script</name>
  <files>install.sh, scripts/verify-mcp-scope.sh</files>
  <action>
    **Source repo layout:** The project repo at `/Users/aash-zsbch1500/Desktop/Github projects/SEO/` is the source. Create a directory layout that mirrors the target `~/.claude/` structure:
    - `skills/` — contains all skill directories (copy existing 13 from `~/.claude/skills/` into source repo)
    - `agents/` — contains all agent .md files (copy existing 6 seo-* agents from `~/.claude/agents/` into source repo)

    First, copy the existing skill and agent files into the source repo so they are version-controlled:
    ```bash
    cp -R ~/.claude/skills/seo* skills/
    cp ~/.claude/agents/seo-*.md agents/
    ```

    **install.sh:** Create a bash script at repo root that:
    1. Accepts optional `--dry-run` flag to show what would be copied without copying
    2. Validates all SKILL.md files using `skills/seo/hooks/validate-yaml.py` BEFORE copying — exits non-zero if any fail (this satisfies the "YAML validation catches errors before deployment" success criterion)
    3. Copies all directories from `skills/` to `~/.claude/skills/` (using `cp -R`, preserving structure)
    4. Copies all `.md` files from `agents/` to `~/.claude/agents/`
    5. Creates Python venv at `~/.claude/skills/seo/.venv/` if it doesn't exist
    6. Activates the venv and runs `pip install -r ~/.claude/skills/seo/requirements.txt`
    7. Runs `scripts/verify-mcp-scope.sh` to check MCP registration
    8. Prints summary of what was installed (N skills, N agents, venv status, MCP status)

    The script must be idempotent — running it twice produces the same result.
    Use `#!/usr/bin/env bash` and `set -euo pipefail`.

    **scripts/verify-mcp-scope.sh:** Create a script that:
    1. Checks if `~/.claude/mcp.json` exists
    2. Verifies it contains GSC MCP entry (look for "google-search-console" or "gsc" key)
    3. Checks that no `.claude/mcp.json` exists in project directories that would shadow user-scope (warn if found)
    4. Reports status: "GSC MCP: registered at user scope" or "GSC MCP: NOT FOUND — register manually"
    5. Ahrefs MCP is a Claude.ai MCP (not local) — script should note "Ahrefs MCP: available via Claude.ai (no local registration needed)"
    6. Exits 0 always (MCP registration is informational, not blocking)
  </action>
  <verify>
    <automated>cd "/Users/aash-zsbch1500/Desktop/Github projects/SEO" && bash install.sh --dry-run 2>&1 | grep -c "would copy\|validated\|SKILL.md"</automated>
  </verify>
  <done>
    - install.sh exists, is executable, and --dry-run completes without errors
    - Source repo contains skills/ and agents/ directories with all existing SEO files
    - verify-mcp-scope.sh reports MCP registration status
  </done>
</task>

<task type="auto">
  <name>Task 2: Create YAML validation script and verify Python helpers</name>
  <files>skills/seo/hooks/validate-yaml.py, skills/seo/requirements.txt</files>
  <action>
    **validate-yaml.py:** Create (or replace existing) a Python script that:
    1. Accepts file paths as arguments, OR if no args, finds all `SKILL.md` files under the given root directory (default: current directory's `skills/`)
    2. For each SKILL.md:
       - Opens the file and extracts YAML frontmatter between `---` delimiters
       - Parses with `yaml.safe_load()`
       - Validates required fields exist: `name` (string), `description` (string)
       - Validates optional fields if present: `allowed-tools` (list of strings)
       - Validates `name` matches directory name (e.g., `seo-audit/SKILL.md` must have `name: seo-audit`)
       - Validates `description` is non-empty and under 500 chars
    3. Prints pass/fail per file with specific error messages
    4. Exits 0 if all pass, exits 1 if any fail
    5. Use only stdlib (no external deps needed — `yaml` is PyYAML which should be in requirements.txt)

    **requirements.txt:** Read the existing file. Ensure it includes:
    - `PyYAML>=6.0` (for YAML validation)
    - `requests>=2.28` (for fetch_page.py)
    - `beautifulsoup4>=4.12` (for parse_html.py)
    - `lxml>=4.9` (for parse_html.py HTML parser)
    - Any other deps the existing Python scripts import

    **Verify Python helpers:** After venv setup, run each existing Python script with `--help` or as a module import check:
    ```bash
    source ~/.claude/skills/seo/.venv/bin/activate
    python -c "import scripts.fetch_page" 2>/dev/null || python -c "exec(open('scripts/fetch_page.py').read())"
    ```
    The goal is confirming no ImportError — the scripts can be loaded. If any script has import issues, fix the requirements.txt to include the missing dependency.
  </action>
  <verify>
    <automated>cd "/Users/aash-zsbch1500/Desktop/Github projects/SEO" && python3 skills/seo/hooks/validate-yaml.py skills/seo/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - validate-yaml.py exits 0 on valid SKILL.md files and exits 1 on malformed ones
    - requirements.txt includes all dependencies needed by existing Python scripts
    - A test with intentionally broken YAML (missing name field) triggers exit 1
  </done>
</task>

</tasks>

<verification>
1. `bash install.sh --dry-run` completes without errors, listing all files that would be installed
2. `python3 skills/seo/hooks/validate-yaml.py skills/*/SKILL.md` passes for all existing 13 skill directories
3. Create a temp SKILL.md with missing `name:` field — `python3 skills/seo/hooks/validate-yaml.py /tmp/bad-SKILL.md` exits 1
4. `bash scripts/verify-mcp-scope.sh` reports MCP status without errors
</verification>

<success_criteria>
- install.sh is executable and copies skills + agents to ~/.claude/ correctly
- YAML validation catches malformed frontmatter before deployment (exits non-zero)
- Python venv is created with all required dependencies
- All 4 existing Python helper scripts have their dependencies satisfied
- MCP scope verification reports registration status
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
