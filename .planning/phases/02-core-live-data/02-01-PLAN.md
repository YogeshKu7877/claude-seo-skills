---
phase: 02-core-live-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/seo/SKILL.md
  - skills/seo/references/gsc-api-reference.md
  - skills/seo-markdown-audit/SKILL.md
autonomous: true
requirements:
  - LOCAL-01

must_haves:
  truths:
    - "The routing table in seo/SKILL.md matches REQUIREMENTS.md command names for all 9 GSC and 10 Ahrefs Phase 2 commands"
    - "gsc-api-reference.md contains verified GSC MCP tool names discovered via ToolSearch, not TBD placeholders"
    - "/seo markdown-audit <path> analyzes a local markdown file and returns SEO improvement recommendations with no MCP connection"
  artifacts:
    - path: "skills/seo/SKILL.md"
      provides: "Updated routing table with corrected GSC sub-command names"
      contains: "seo-gsc-brand-vs-nonbrand"
    - path: "skills/seo/references/gsc-api-reference.md"
      provides: "Verified GSC MCP tool names and property format"
      contains: "query_search_analytics"
    - path: "skills/seo-markdown-audit/SKILL.md"
      provides: "Markdown SEO audit sub-skill with 10 check categories"
      min_lines: 80
  key_links:
    - from: "skills/seo/SKILL.md"
      to: "skills/seo-markdown-audit/SKILL.md"
      via: "routing table entry"
      pattern: "seo-markdown-audit"
    - from: "skills/seo/references/gsc-api-reference.md"
      to: "GSC MCP tools"
      via: "ToolSearch discovery"
      pattern: "mcp__.*__query_search_analytics"
---

<objective>
Fix the routing table discrepancy between seo/SKILL.md and REQUIREMENTS.md, update gsc-api-reference.md with verified tool names from the GSC MCP, and build the seo-markdown-audit sub-skill.

Purpose: The routing table must match the actual commands being built in Phase 2 (not the placeholder names from initial Phase 1 creation). The GSC reference must have real tool names before any GSC sub-skills can be built. The markdown-audit is the simplest sub-skill (no MCP) and establishes the pattern for all Phase 2 sub-skills.

Output: Updated seo/SKILL.md routing table, verified gsc-api-reference.md, working seo-markdown-audit/SKILL.md
</objective>

<execution_context>
@/Users/aash-zsbch1500/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aash-zsbch1500/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-live-data/02-RESEARCH.md
@skills/seo/SKILL.md
@skills/seo/references/gsc-api-reference.md
@skills/seo/references/mcp-degradation.md

<interfaces>
<!-- Routing table structure from seo/SKILL.md — executor must update these sections -->

From skills/seo/SKILL.md (Level 2 routing, GSC section to update):
- `/seo gsc pages <site>` → REMOVE (fold into overview)
- `/seo gsc queries <site>` → REMOVE (fold into overview)
- `/seo gsc sitemaps <site>` → REMOVE (defer to Phase 3)
- ADD: `/seo gsc brand-vs-nonbrand <site>` → load `seo-gsc-brand-vs-nonbrand/SKILL.md`
- ADD: `/seo gsc content-decay <site>` → load `seo-gsc-content-decay/SKILL.md`
- ADD: `/seo gsc new-keywords <site>` → load `seo-gsc-new-keywords/SKILL.md`
- RENAME: `/seo gsc indexing` → `/seo gsc index-issues` (directory stays `seo-gsc-indexing`)

From skills/seo/SKILL.md (Quick Reference table, Phase 2 GSC section to update):
- Remove rows: `gsc pages`, `gsc queries`, `gsc sitemaps`
- Add rows: `gsc brand-vs-nonbrand`, `gsc content-decay`, `gsc new-keywords`
- Rename: `gsc indexing` → `gsc index-issues`

GSC MCP verified tool names (from 02-RESEARCH.md, source: GSC-MCP/src/index.ts):
- `query_search_analytics` (siteUrl, startDate, endDate, dimensions, rowLimit, filters)
- `inspect_url` (siteUrl, inspectionUrl)
- `find_keyword_opportunities` (siteUrl, startDate, endDate, minImpressions, maxCtr, maxPosition)
- `compare_performance` (siteUrl, currentStartDate, currentEndDate, previousStartDate, previousEndDate, dimension, limit)
- `analyze_brand_queries` (siteUrl, startDate, endDate, brandTerms)
- `list_sites` (account)
- `list_accounts` ()
- `get_top_pages` (siteUrl, startDate, endDate, sortBy, limit)
- `get_keyword_trend` (siteUrl, keyword, startDate, endDate)
- `export_analytics` (siteUrl, startDate, endDate, dimensions, format, rowLimit, searchType)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix routing table discrepancy in seo/SKILL.md</name>
  <files>skills/seo/SKILL.md</files>
  <action>
Update seo/SKILL.md to align the GSC routing with REQUIREMENTS.md command names. The routing table is the installed artifact that all sub-skills depend on.

**Phase 2 Commands table (Quick Reference):**
Remove these rows:
- `/seo gsc pages <site>` — Top performing pages by clicks
- `/seo gsc queries <site>` — Top performing queries and brand vs non-brand split
- `/seo gsc sitemaps <site>` — Sitemap submission and coverage status

Add these rows (in alphabetical order within GSC section):
- `/seo gsc brand-vs-nonbrand <site>` — Brand vs non-brand traffic split analysis | Phase 2
- `/seo gsc content-decay <site>` — Pages losing rankings over 90 days | Phase 2
- `/seo gsc new-keywords <site>` — Keywords started ranking for recently | Phase 2

Rename:
- `/seo gsc indexing <site>` → `/seo gsc index-issues <site>` (keep description: "Indexing coverage report" → update to "Pages not indexed with reasons")

**Level 2 Sub-command Routing (GSC section):**
Remove:
- `/seo gsc pages <site>` → load `seo-gsc-pages/SKILL.md`
- `/seo gsc queries <site>` → load `seo-gsc-queries/SKILL.md`
- `/seo gsc sitemaps <site>` → load `seo-gsc-sitemaps/SKILL.md`

Add:
- `/seo gsc brand-vs-nonbrand <site>` → load `seo-gsc-brand-vs-nonbrand/SKILL.md`
- `/seo gsc content-decay <site>` → load `seo-gsc-content-decay/SKILL.md`
- `/seo gsc new-keywords <site>` → load `seo-gsc-new-keywords/SKILL.md`

Rename:
- `/seo gsc indexing <site>` → `/seo gsc index-issues <site>` → load `seo-gsc-indexing/SKILL.md` (keep directory name as `seo-gsc-indexing` since that's already in the routing table; just change the user-facing command name)

**Routing Table (full mapping):**
Same changes — remove `gsc pages`, `gsc queries`, `gsc sitemaps` rows; add `gsc brand-vs-nonbrand`, `gsc content-decay`, `gsc new-keywords`; rename `gsc indexing` display to `gsc index-issues`.

**Sub-Skills list (numbered list at bottom):**
Update Phase 2 GSC list items:
- Remove: seo-gsc-pages, seo-gsc-queries, seo-gsc-sitemaps
- Add: seo-gsc-brand-vs-nonbrand, seo-gsc-content-decay, seo-gsc-new-keywords
- Rename display: seo-gsc-indexing description from "Indexing coverage report" to "Pages not indexed with reasons"
- Renumber if needed to keep sequential

After editing, run validate-yaml.py to confirm frontmatter still passes the 1000-char limit.
  </action>
  <verify>
    <automated>~/.claude/skills/seo/.venv/bin/python skills/seo/hooks/validate-yaml.py skills/seo/SKILL.md && grep -c "brand-vs-nonbrand" skills/seo/SKILL.md | xargs test 3 -le</automated>
  </verify>
  <done>Routing table has exactly 9 GSC entries matching REQUIREMENTS.md names (overview, drops, opportunities, cannibalization, index-issues, compare, brand-vs-nonbrand, content-decay, new-keywords). No gsc-pages, gsc-queries, or gsc-sitemaps entries remain. YAML validation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update gsc-api-reference.md with verified tool names</name>
  <files>skills/seo/references/gsc-api-reference.md</files>
  <action>
Replace the TBD placeholder mapping table in gsc-api-reference.md with verified tool names from the GSC MCP source code (confirmed in 02-RESEARCH.md from reading `/Users/aash-zsbch1500/Desktop/GSC-MCP/GSC-MCP/src/index.ts`).

**Replace the placeholder mapping table with:**

| Sub-command | GSC MCP Tool | Key Params | Verified |
|-------------|-------------|-----------|----------|
| `overview` | `query_search_analytics` (x2: query + page dimensions) | siteUrl, startDate, endDate, dimensions, rowLimit | Yes (source) |
| `drops` | `compare_performance` | siteUrl, 4 date params, dimension=page | Yes (source) |
| `opportunities` | `find_keyword_opportunities` | siteUrl, startDate, endDate, minImpressions, maxCtr, maxPosition | Yes (source) |
| `cannibalization` | `query_search_analytics` | siteUrl, dates, dimensions=["query","page"] | Yes (source) |
| `index-issues` | `inspect_url` (per URL, cap at 20) | siteUrl, inspectionUrl | Yes (source) |
| `compare` | `compare_performance` | siteUrl, 4 date params, dimension | Yes (source) |
| `brand-vs-nonbrand` | `analyze_brand_queries` | siteUrl, dates, brandTerms (user-provided) | Yes (source) |
| `content-decay` | `compare_performance` | siteUrl, 90-day window, dimension=page | Yes (source) |
| `new-keywords` | `compare_performance` | siteUrl, 28-day window, filter previousClicks=0 | Yes (source) |

**Update the property format section:**
Change "UNVERIFIED" to "VERIFIED (from source)" — both `sc-domain:example.com` and `https://example.com` are accepted. Keep the user-facing guidance about which format to use.

**Add a new section "Additional GSC Tools Available" listing:**
- `list_accounts` — no params, returns connected Google accounts
- `list_sites` — optional `account`, returns properties for account
- `get_top_pages` — siteUrl, startDate, endDate, sortBy, limit — used within overview
- `get_keyword_trend` — siteUrl, keyword, startDate, endDate — for keyword-specific drilldown
- `export_analytics` — siteUrl, startDate, endDate, dimensions, format — data export

**Update the MCP availability check section:**
Keep the ToolSearch approach but note: "The GSC MCP server is registered as `gsc-mcp-server` in its package.json. The tool name prefix depends on the alias used during registration in `~/.claude/mcp.json`. Use ToolSearch to discover the actual prefix at runtime."

**Remove TBD markers and update the "Phase 2 Discovery Checklist":**
Check off items that are now verified from source: tool names, property format. Leave unchecked: response field names (need live testing), row limit behavior (need live testing), registered MCP alias (need runtime discovery).

**Add CTR display rule:**
Document that `query_search_analytics` returns CTR as decimal (0.0523) — multiply by 100 for display (5.23%). The GSC MCP's formatted output converts this, but raw API responses do not.

Source: 02-RESEARCH.md "GSC MCP Tool Name Verified Mapping" section (HIGH confidence, read from TypeScript source).
  </action>
  <verify>
    <automated>grep -c "TBD" skills/seo/references/gsc-api-reference.md | xargs test 0 -eq && grep "query_search_analytics" skills/seo/references/gsc-api-reference.md | head -1</automated>
  </verify>
  <done>gsc-api-reference.md has zero TBD placeholders. All 9 sub-commands mapped to verified GSC MCP tool names. Property format marked as verified. CTR display rule documented.</done>
</task>

<task type="auto">
  <name>Task 3: Create seo-markdown-audit sub-skill</name>
  <files>skills/seo-markdown-audit/SKILL.md</files>
  <action>
Create `skills/seo-markdown-audit/SKILL.md` — a self-contained sub-skill with zero MCP dependency. This is the simplest Phase 2 command and establishes the file pattern for all other new sub-skills.

**Frontmatter:**
```yaml
---
name: seo-markdown-audit
description: >
  Audit markdown files for SEO before publishing. Checks heading structure,
  meta description, keyword density, content length, link quality, image alt
  text, and frontmatter completeness. Use when user says "markdown audit",
  "check my markdown", "SEO check markdown", or "audit this post".
allowed-tools:
  - Read
  - Bash
  - Glob
---
```

**Body structure:**

# Markdown SEO Audit

Analyzes a local markdown file for SEO issues before publishing. No MCP connection required.

## Inputs

- `path`: Absolute path to the markdown file to audit.
  If the user provides a relative path, resolve it using `Bash: realpath <path>` before calling Read.
  If the user provides a directory, use `Glob: **/*.md` to find all markdown files and let the user choose.

## Execution

Use the Read tool to load the file content, then run these 10 check categories:

**1. Title / H1 Check**
- Count lines starting with `# ` (H1) — flag if 0 or >1
- Check H1 character length — warn if >60 chars (Google truncates around 60)
- Extract primary keyword from H1 for use in other checks

**2. Meta Description (Frontmatter)**
- If YAML frontmatter exists (between `---` markers), check for `description` field
- Flag if missing, warn if <120 chars or >160 chars (optimal: 150-160)
- Check if description contains the primary keyword from H1

**3. Heading Hierarchy**
- Verify headings follow proper nesting: H1 → H2 → H3 (no H3 before first H2, no skipped levels)
- Flag duplicate heading text (exact same heading appearing twice)
- Warn if no H2 headings exist (flat content with no structure)

**4. Keyword Placement**
- Extract primary keyword from H1 (longest noun phrase or first 3 meaningful words)
- Check if keyword appears in first 100 words of body content (after H1)
- Check if keyword appears in at least one H2
- Do NOT calculate keyword density percentage (outdated SEO practice)

**5. Content Length**
- Count words (split on whitespace, exclude frontmatter and code blocks)
- Flag CRITICAL if <300 words
- Warn if <800 words (thin content for most topics)
- Note if >3,000 words (consider splitting into pillar + sub-pages)

**6. Readability**
- Calculate average sentence length (split on `.`, `!`, `?`)
- Warn if average >25 words per sentence (too complex for most audiences)
- Flag paragraphs >5 sentences without a subheading break

**7. Link Analysis**
- Find all `[text](url)` patterns
- Flag generic anchor text: "here", "click here", "this link", "read more", "link"
- Count internal links (relative paths starting with `/` or `./` or `../`) — warn if zero
- Count external links — note if zero (no external references can hurt E-E-A-T)

**8. Image Analysis**
- Find all `![alt](url)` patterns
- Flag empty alt text: `![](url)`
- Flag generic alt text: "image", "photo", "screenshot", "picture", "img"
- Note total image count — warn if zero for content >500 words

**9. Frontmatter Completeness**
- Check for YAML frontmatter presence (between `---` markers at top of file)
- Required fields: `title` (flag if missing), `description` (flag if missing)
- Recommended fields: `date`, `author`, `tags`/`categories`
- Check `title` length — warn if >60 chars

**10. Technical Issues**
- Check for broken markdown syntax: unclosed links `[text](`, unclosed images `![alt](`
- Check for raw HTML (not recommended in markdown for SEO — search engines may not parse consistently)
- Check for consecutive blank lines >2 (formatting issue)

## Output Format

```
## Markdown SEO Audit: [filename]

**Overall Score: X/10** (count of categories with no critical/high issues)

### Critical Issues
- [List of critical issues with line numbers if possible]

### High Priority
- [Issues that significantly impact SEO]

### Medium Priority
- [Optimization opportunities]

### Low Priority / Suggestions
- [Nice-to-have improvements]

### Summary
| Check | Status | Notes |
|-------|--------|-------|
| Title/H1 | PASS/WARN/FAIL | [brief note] |
| Meta Description | PASS/WARN/FAIL | [brief note] |
| Heading Hierarchy | PASS/WARN/FAIL | [brief note] |
| Keyword Placement | PASS/WARN/FAIL | [brief note] |
| Content Length | PASS/WARN/FAIL | [word count] |
| Readability | PASS/WARN/FAIL | [avg sentence length] |
| Links | PASS/WARN/FAIL | [internal/external count] |
| Images | PASS/WARN/FAIL | [count, alt text status] |
| Frontmatter | PASS/WARN/FAIL | [fields present/missing] |
| Technical | PASS/WARN/FAIL | [issues found] |
```
  </action>
  <verify>
    <automated>test -f skills/seo-markdown-audit/SKILL.md && ~/.claude/skills/seo/.venv/bin/python skills/seo/hooks/validate-yaml.py skills/seo-markdown-audit/SKILL.md && grep -c "## " skills/seo-markdown-audit/SKILL.md | xargs test 5 -le</automated>
  </verify>
  <done>skills/seo-markdown-audit/SKILL.md exists, passes YAML validation, contains all 10 check categories, has proper frontmatter with name and description, and requires no MCP dependency.</done>
</task>

</tasks>

<verification>
1. `grep -c "brand-vs-nonbrand\|content-decay\|new-keywords" skills/seo/SKILL.md` returns 9+ (3 entries x 3 locations each)
2. `grep -c "pages\|queries\|sitemaps" skills/seo/SKILL.md` — verify "gsc pages", "gsc queries", "gsc sitemaps" are NOT in routing table (may appear in other contexts like Phase 3 references)
3. `grep -c "TBD" skills/seo/references/gsc-api-reference.md` returns 0
4. `grep "query_search_analytics" skills/seo/references/gsc-api-reference.md` returns matches
5. `test -f skills/seo-markdown-audit/SKILL.md` succeeds
6. `~/.claude/skills/seo/.venv/bin/python skills/seo/hooks/validate-yaml.py skills/seo/SKILL.md skills/seo-markdown-audit/SKILL.md` passes
</verification>

<success_criteria>
- seo/SKILL.md routing table has exactly 9 GSC commands matching REQUIREMENTS.md names
- gsc-api-reference.md has verified tool names with zero TBD placeholders
- seo-markdown-audit/SKILL.md is a complete, self-contained sub-skill with 10 check categories
- All YAML frontmatter validates correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-live-data/02-01-SUMMARY.md`
</output>
