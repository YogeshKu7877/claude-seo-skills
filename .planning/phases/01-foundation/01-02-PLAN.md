---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - skills/seo/SKILL.md
  - skills/seo/references/mcp-degradation.md
  - skills/seo/references/ahrefs-api-reference.md
  - skills/seo/references/gsc-api-reference.md
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "Invoking /seo with any of the 27 command names routes to the correct sub-skill or returns a clear 'not yet available' message for Phase 2+ commands"
    - "Invoking a command that requires an unavailable MCP returns a human-readable error with a fallback suggestion"
    - "Reference files (CWV, E-E-A-T, quality gates, schema types, mcp-degradation, ahrefs-api, gsc-api) are loaded on-demand, not at startup"
    - "Ahrefs monetary values documented as requiring division by 100 (cents to USD) in the shared reference convention"
  artifacts:
    - path: "skills/seo/SKILL.md"
      provides: "Main orchestrator with 27-command routing table and two-level routing"
      contains: "gsc"
      min_lines: 150
    - path: "skills/seo/references/mcp-degradation.md"
      provides: "Error templates, fallback mapping, and MCP availability check patterns"
      contains: "fallback"
    - path: "skills/seo/references/ahrefs-api-reference.md"
      provides: "Ahrefs MCP tool names, response fields, and cents-to-USD convention"
      contains: "divide by 100"
    - path: "skills/seo/references/gsc-api-reference.md"
      provides: "GSC MCP tool names, property format expectations, and response fields"
      contains: "sc-domain"
  key_links:
    - from: "skills/seo/SKILL.md"
      to: "skills/seo-gsc-*/SKILL.md"
      via: "two-level routing table"
      pattern: "gsc.*seo-gsc"
    - from: "skills/seo/SKILL.md"
      to: "skills/seo/references/mcp-degradation.md"
      via: "on-demand @reference"
      pattern: "references/mcp-degradation"
    - from: "skills/seo/references/ahrefs-api-reference.md"
      to: "Ahrefs MCP tools"
      via: "tool name mapping"
      pattern: "mcp__claude_ai_ahrefs"
---

<objective>
Expand the main seo/SKILL.md orchestrator with a 27-command routing table using two-level routing, create MCP degradation reference with error templates and fallback mappings, create Ahrefs and GSC API reference files with the cents-to-USD convention, and ensure all reference files follow the on-demand loading pattern.

Purpose: Make every command routable (existing 12 work immediately, 15 new commands return clear "not yet available" messages until Phase 2+), establish consistent MCP error handling patterns, and document the Ahrefs monetary conversion so no future command ever displays raw cents.
Output: Updated SKILL.md, mcp-degradation.md, ahrefs-api-reference.md, gsc-api-reference.md
</objective>

<execution_context>
@/Users/aash-zsbch1500/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aash-zsbch1500/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Current seo/SKILL.md structure (to be expanded, not replaced) -->
From skills/seo/SKILL.md:
  - YAML frontmatter: name, description, allowed-tools
  - Quick Reference table: 12 commands
  - Orchestration Logic: audit delegation flow
  - Industry Detection: business type signals
  - Quality Gates: thin content thresholds
  - Reference Files: on-demand loading list
  - Scoring Methodology: SEO Health Score weights
  - Sub-Skills: list of 12
  - Subagents: list of 6

<!-- Existing reference file pattern -->
Reference files at skills/seo/references/:
  cwv-thresholds.md    — Core Web Vitals thresholds
  eeat-framework.md    — E-E-A-T evaluation criteria
  quality-gates.md     — Content quality thresholds
  schema-types.md      — Schema.org type reference

<!-- MCP tool naming patterns (from Ahrefs MCP tools visible in environment) -->
Ahrefs MCP tools follow pattern: mcp__claude_ai_ahrefs__<endpoint-name>
Examples:
  mcp__claude_ai_ahrefs__site-explorer-metrics
  mcp__claude_ai_ahrefs__site-explorer-domain-rating
  mcp__claude_ai_ahrefs__site-explorer-all-backlinks
  mcp__claude_ai_ahrefs__site-explorer-organic-keywords
  mcp__claude_ai_ahrefs__site-explorer-organic-competitors
  mcp__claude_ai_ahrefs__keywords-explorer-overview
  mcp__claude_ai_ahrefs__site-explorer-broken-backlinks
  mcp__claude_ai_ahrefs__site-explorer-referring-domains
  mcp__claude_ai_ahrefs__site-explorer-top-pages
  mcp__claude_ai_ahrefs__site-explorer-domain-rating-history
  mcp__claude_ai_ahrefs__brand-radar-mentions-overview

GSC MCP: Local server at /Users/aash-zsbch1500/Desktop/GSC-MCP
  - Tool names TBD (need to discover during Phase 2)
  - Property format: likely "sc-domain:example.com" or "https://example.com" (unverified, noted as Phase 2 blocker)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand seo/SKILL.md with 27-command routing table</name>
  <files>skills/seo/SKILL.md</files>
  <action>
    Read the existing `skills/seo/SKILL.md` (already in source repo from Plan 01). Expand it while preserving ALL existing content (scoring methodology, industry detection, quality gates, etc.). Make these specific changes:

    **1. Update Quick Reference table** — Expand from 12 to 27 commands. Group by category:

    | Command | What it does | Status |
    |---------|-------------|--------|
    | (existing 12 — keep as-is, status: active) | ... | active |
    | `/seo gsc <sub-command> <site>` | Google Search Console analysis (9 sub-commands) | Phase 2 |
    | `/seo ahrefs <sub-command> <domain>` | Ahrefs analysis (10 sub-commands) | Phase 2 |
    | `/seo markdown-audit <path>` | Markdown SEO audit (no MCP needed) | Phase 2 |
    | `/seo serp <keyword>` | Live SERP analysis | Phase 3 |
    | `/seo content-brief <keyword>` | AI content brief from SERP data | Phase 3 |
    | `/seo brand-radar <brand>` | AI search brand monitoring | Phase 3 |
    | `/seo site-audit-pro <domain>` | Flagship multi-MCP audit | Phase 3 |
    | `/seo report <type> <domain>` | Automated report generation | Phase 3 |

    **2. Add two-level routing section** — After the Orchestration Logic section, add:

    ```markdown
    ## Command Routing

    ### Level 1: Command Group Detection
    When the user invokes `/seo <command>`:
    1. Match against the routing table below
    2. If exact match → load the sub-skill directly
    3. If group match (gsc, ahrefs) → extract sub-command and route to specific sub-skill
    4. If no match → suggest closest command from table

    ### Level 2: Sub-command Routing
    For grouped commands like `gsc` and `ahrefs`:
    - `/seo gsc overview` → load `seo-gsc-overview/SKILL.md`
    - `/seo gsc drops` → load `seo-gsc-drops/SKILL.md`
    - (list all 9 GSC sub-commands)
    - `/seo ahrefs overview` → load `seo-ahrefs-overview/SKILL.md`
    - (list all 10 Ahrefs sub-commands)

    ### Routing Table
    (Full table mapping all 27 commands to their sub-skill directory names)
    ```

    **3. Add "Not Yet Available" handling** — For commands with status "Phase 2" or later:
    ```markdown
    ### Unavailable Command Response
    If a command is marked as Phase 2+ and its sub-skill directory does not exist:
    1. Tell the user: "[command] is planned for Phase N and not yet available."
    2. Suggest an alternative from the active commands that provides partial value.
    3. Do NOT attempt to fabricate the functionality inline.
    ```

    **4. Add MCP error handling reference** — In the Reference Files section, add:
    ```markdown
    - `references/mcp-degradation.md` — MCP availability checks, error templates, fallback mapping
    - `references/ahrefs-api-reference.md` — Ahrefs MCP tool mapping, response fields, monetary conversion
    - `references/gsc-api-reference.md` — GSC MCP tool mapping, property formats, response fields
    ```

    **5. Update description** — Expand the YAML frontmatter description to mention 27 commands, GSC, and Ahrefs. Update "Orchestrates 12 specialized sub-skills" to "Orchestrates 27 specialized sub-skills (12 active, 15 planned)".

    **6. Update Sub-Skills list** — Add entries 13-27 with "(Phase N)" annotations.

    Per user decision: "Logic lives in each sub-skill's SKILL.md (not in agent files) — agents only used for parallel audit work." Ensure routing instructions reflect this.
  </action>
  <verify>
    <automated>cd "/Users/aash-zsbch1500/Desktop/Github projects/SEO" && grep -c "seo-gsc\|seo-ahrefs\|markdown-audit\|serp\|content-brief\|brand-radar\|site-audit-pro\|report" skills/seo/SKILL.md</automated>
  </verify>
  <done>
    - SKILL.md contains all 27 commands in the routing table
    - Two-level routing for gsc and ahrefs sub-commands is documented
    - Phase 2+ commands have clear "not yet available" handling instructions
    - Existing 12 commands, scoring, industry detection, quality gates are preserved
    - YAML frontmatter is valid (name: seo, description updated)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MCP degradation, Ahrefs API, and GSC API reference files</name>
  <files>skills/seo/references/mcp-degradation.md, skills/seo/references/ahrefs-api-reference.md, skills/seo/references/gsc-api-reference.md</files>
  <action>
    **mcp-degradation.md:** Create a reference file that all future MCP-dependent skills will @-reference. Include:

    1. **MCP Availability Check Pattern** — How to detect if an MCP is available:
       - Ahrefs: Try `ToolSearch` for `+ahrefs` — if tools returned, Ahrefs MCP is available
       - GSC: Try `ToolSearch` for `+google-search-console` or check `~/.claude/mcp.json` for GSC entry
       - WebMCP: Try `ToolSearch` for `+webmcp` — if tools returned, WebMCP is available

    2. **Error Template** — Standard format for MCP unavailability:
       ```
       ## ⚠️ {MCP Name} Not Available

       The {command} command requires {MCP Name} MCP, which is not currently connected.

       **What you can do:**
       - {Fallback suggestion 1 — alternative command that works without this MCP}
       - {Fallback suggestion 2 — manual alternative}

       **To connect {MCP Name}:**
       - {Setup instructions}
       ```

    3. **Fallback Mapping** — Table of commands and their fallbacks:
       | Command | Requires | Fallback |
       |---------|----------|----------|
       | `/seo gsc *` | GSC MCP | Use `/seo technical` for static analysis |
       | `/seo ahrefs *` | Ahrefs MCP | Use `/seo audit` for static analysis |
       | `/seo serp` | Ahrefs + WebMCP | Ahrefs-only mode if WebMCP unavailable |
       | `/seo content-brief` | Ahrefs + GSC | Works with Ahrefs only, GSC enriches |
       | `/seo brand-radar` | Ahrefs | No fallback — Ahrefs required |
       | `/seo site-audit-pro` | All MCPs | Runs with available MCPs, skips unavailable |
       | `/seo report` | Varies by type | Generates report with available data only |

    4. **Self-contained check** — Per user decision: "MCP availability checks happen inside each sub-skill (self-contained, not at orchestrator level)." Document that each sub-skill should check its own MCP requirements at the start of execution using the patterns above.

    **ahrefs-api-reference.md:** Create reference file with:

    1. **Tool Name Mapping** — Map each `/seo ahrefs` sub-command to the actual MCP tool(s) it calls:
       | Sub-command | MCP Tool(s) |
       |-------------|-------------|
       | overview | `mcp__claude_ai_ahrefs__site-explorer-metrics`, `mcp__claude_ai_ahrefs__site-explorer-domain-rating` |
       | backlinks | `mcp__claude_ai_ahrefs__site-explorer-all-backlinks` |
       | keywords | `mcp__claude_ai_ahrefs__site-explorer-organic-keywords` |
       | competitors | `mcp__claude_ai_ahrefs__site-explorer-organic-competitors` |
       | content-gap | `mcp__claude_ai_ahrefs__keywords-explorer-matching-terms` (filtered by competitor) |
       | broken-links | `mcp__claude_ai_ahrefs__site-explorer-broken-backlinks` |
       | new-links | `mcp__claude_ai_ahrefs__site-explorer-referring-domains` (date-filtered) |
       | anchor-analysis | `mcp__claude_ai_ahrefs__site-explorer-anchors` |
       | dr-history | `mcp__claude_ai_ahrefs__site-explorer-domain-rating-history` |
       | top-pages | `mcp__claude_ai_ahrefs__site-explorer-top-pages` |

    2. **CRITICAL: Monetary Value Convention (FOUND-06)**:
       ```
       ## ⚠️ Ahrefs Monetary Values — ALWAYS Divide by 100

       All monetary values from Ahrefs MCP are returned in CENTS, not dollars.
       Before displaying ANY monetary value to the user:
         - Divide by 100
         - Format as USD: "$X,XXX"

       Example:
         - API returns: traffic_cost = 125000
         - Display as: "$1,250"

       This applies to: traffic_cost, cpc, traffic_value, and any field ending in _cost or _value.
       NEVER display raw cents to the user.
       ```

    3. **Common Response Fields** — Document key fields from Ahrefs API responses that sub-skills will need (domain_rating, backlinks, organic_keywords, organic_traffic, traffic_cost, etc.)

    4. **Rate Limiting** — Note that Ahrefs MCP has API unit costs per call. Document: "Be mindful of API usage. Avoid redundant calls. Cache results within a session where possible."

    **gsc-api-reference.md:** Create reference file with:

    1. **Tool Name Mapping** — Placeholder structure (actual tool names to be discovered in Phase 2):
       ```
       ## GSC MCP Tool Mapping (to be verified in Phase 2)

       GSC MCP source: /Users/aash-zsbch1500/Desktop/GSC-MCP
       Tool name pattern: TBD — must be discovered when GSC commands are built

       Placeholder mapping:
       | Sub-command | Expected MCP Tool | Verified |
       |-------------|-------------------|----------|
       | overview | TBD | No |
       | drops | TBD | No |
       | ... | ... | No |
       ```

    2. **Property Format Warning**:
       ```
       ## ⚠️ GSC Property Format — UNVERIFIED

       The GSC MCP property format has NOT been verified.
       Possible formats:
         - "sc-domain:example.com" (domain property)
         - "https://example.com" (URL prefix property)

       MUST be tested before building GSC commands in Phase 2.
       See STATE.md blocker: "GSC MCP property format unverified"
       ```

    3. **Expected Response Fields** — Standard GSC metrics: clicks, impressions, ctr, position, query, page, date.

    All three reference files should follow the existing pattern: markdown with a date header comment `<!-- Updated: YYYY-MM-DD -->` at the top.
  </action>
  <verify>
    <automated>cd "/Users/aash-zsbch1500/Desktop/Github projects/SEO" && test -f skills/seo/references/mcp-degradation.md && test -f skills/seo/references/ahrefs-api-reference.md && test -f skills/seo/references/gsc-api-reference.md && grep -l "divide by 100\|CENTS" skills/seo/references/ahrefs-api-reference.md && echo "ALL PASS"</automated>
  </verify>
  <done>
    - mcp-degradation.md has check patterns, error templates, and fallback mapping for all MCP-dependent commands
    - ahrefs-api-reference.md maps all 10 sub-commands to MCP tool names and documents the cents-to-USD convention
    - gsc-api-reference.md has placeholder structure with property format warning
    - All three files follow the existing reference file pattern with date headers
    - Files are loaded on-demand (referenced in SKILL.md but not auto-loaded)
  </done>
</task>

</tasks>

<verification>
1. `grep -c "routing\|route" skills/seo/SKILL.md` returns 5+ (routing table is present)
2. `grep "27" skills/seo/SKILL.md` confirms 27-command count is mentioned
3. `grep "divide by 100" skills/seo/references/ahrefs-api-reference.md` confirms monetary convention
4. `grep "fallback" skills/seo/references/mcp-degradation.md` confirms degradation patterns
5. `grep "UNVERIFIED\|TBD" skills/seo/references/gsc-api-reference.md` confirms honest unknowns
6. Run `python3 skills/seo/hooks/validate-yaml.py skills/seo/SKILL.md` — must pass with updated frontmatter
</verification>

<success_criteria>
- seo/SKILL.md routes all 27 commands via two-level routing table
- Existing 12 commands are unchanged and fully functional
- Phase 2+ commands return clear "not yet available" messages
- MCP degradation reference provides reusable error/fallback patterns
- Ahrefs cents-to-USD convention is documented and referenced
- GSC reference honestly marks unverified items as TBD
- All reference files follow on-demand loading pattern
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
